---
import ServicesList from './ServicesList'
---

<section class="pt-28 pb-20">
  <!-- Scrolling banner -->
  <div class="mb-0 overflow-hidden py-2">
    <div class="ticker-track flex w-max items-start whitespace-nowrap text-6xl font-normal text-palette-950">
      {[1, 2, 3, 4].map(() => (
        <div class="flex shrink-0 items-start">
          <span class="mx-2">OUR SERVICES</span>
          <span class="mx-2 -mt-2 text-8xl font-light text-primary-600">*</span>
        </div>
      ))}
    </div>
  </div>

  <div class="mx-auto max-w-6xl px-5">
    <!-- Services list -->
    <ServicesList client:visible />
  </div>
</section>

<style>
  .ticker-track {
    animation: ticker-scroll 8s linear infinite;
  }

  @keyframes ticker-scroll {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-25%);
    }
  }
</style>
